<form style="display:inline-block;" method="post" action="{{ path('question_delete', {'numQu': question.numQu}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ question.numQu) }}">
    <button class="btn" style="background: #006DF0;color: #fff;">Delete</button>
</form>
